<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Edit Event</title>

  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-image: url('/image/events_background4.png'); /* Change to your correct image path */
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      margin: 0;
      padding: 0;
    }

    /* Dark transparent overlay for form area */
    .overlay {
      background-color: rgba(33, 33, 33, 0.8); /* Dark grey transparent */
      min-height: 100vh;
      padding: 110px 20px 60px 20px; /* top padding matches navbar */
      color: white;
    }

    .form-label,
    .form-control,
    .btn,
    h2 {
      color: white;
    }

    .form-control {
      background-color: rgba(255, 255, 255, 0.1);
      border: 1px solid white;
    }

    .form-control::placeholder {
      color: #ccc;
    }

    .btn-primary {
      background-color: #FE3E4D;
      border-color: #FE3E4D;
    }
  </style>
</head>

<body>

  <!-- Your original navbar (unchanged) -->
  <nav style="position: fixed; top: 0; width: 100%; background-color: transparent; padding: 15px 30px; z-index: 1000;">
    <style>
      nav a:hover {
        text-decoration: underline !important;
      }
    </style>

    <div style="display: flex; justify-content: space-between; align-items: center;">
      <div style="flex: 1; text-align: left;">
        <a href="/" style="color: black; font-size: 30px; font-family: 'Montserrat'; font-weight: bold; text-decoration: none;">
          Singapore IG Connect
        </a>
      </div>

      <div style="position: absolute; left: 50%; transform: translateX(-50%); display: flex; gap: 20px;">
        <a href="/interestgroups" style="color:#FE3E4D; text-decoration: none; font-weight: normal; font-size:15px;">Interest Groups</a>
        <a href="/IGmembers" style="color:#FE3E4D; text-decoration: none; font-weight: normal; font-size:15px;">IG Members</a>
        <a href="/IGevents" style="color:#FE3E4D; text-decoration: none; font-weight: normal; font-size:15px;">IG Events</a>
        <a href="/schools" style="color:#FE3E4D; text-decoration: none; font-weight: normal; font-size:15px;">School Directory</a>
        <a href="/studentprofiles" style="color:#FE3E4D; text-decoration: none; font-weight: normal; font-size:15px;">Student Profiles</a>
        <a href="/IGgalleries" style="color:#FE3E4D; text-decoration: none; font-weight: normal; font-size:15px;">IG Galleries</a>
      </div>
    </div>
  </nav>

  <!-- Form wrapped in dark overlay -->
  <div class="container-fluid overlay">
    <div class="container">
      <div class="text-center">
        <h2>Update Event</h2>
      </div>

      <div class="mt-4">
        <% if (event) { %>
          <form action="/editEvent/<%= event.id %>" method="POST">
            <div class="mb-3">
              <label for="name" class="form-label">Event Name:</label>
              <input
                type="text"
                id="name"
                name="name"
                class="form-control"
                value="<%= event.name %>"
                required
              />
            </div>

            <div class="mb-3">
              <label for="date" class="form-label">Event Date:</label>
              <input
                type="date"
                id="date"
                name="date"
                class="form-control"
                value="<%= event.formattedDate %>"
                required
              />
            </div>

            <div class="mb-3">
              <label for="location" class="form-label">Location:</label>
              <input
                type="text"
                id="location"
                name="location"
                class="form-control"
                value="<%= event.location %>"
                required
              />
            </div>

            <div class="mb-3">
              <label for="description" class="form-label">Description:</label>
              <textarea
                id="description"
                name="description"
                class="form-control"
                required
              ><%= event.description %></textarea>
            </div>

            <button type="submit" class="btn btn-primary">Update</button>
          </form>
        <% } %>
      </div>
    </div>
  </div>

</body>
</html>
