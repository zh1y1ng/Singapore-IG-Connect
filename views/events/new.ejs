<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=Quicksand&display=swap" rel="stylesheet">
  <title>Add Event</title>

  <style>
    /* Background image */
    body {
      font-family: 'Segoe UI', sans-serif;
      background-image: url('/image/events_background4.png'); /* Update path if needed */
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      margin: 0;
      padding: 0;
    }

    /* Dark transparent overlay */
    .overlay {
      background-color: rgba(33, 33, 33, 0.8); /* dark grey transparent */
      min-height: 100vh;
      padding: 60px 20px;
      color: white;
    }

    .form-label,
    .form-control,
    .btn,
    h2 {
      color: white;
    }

    .form-control {
      background-color: rgba(255, 255, 255, 0.1);
      border: 1px solid white;
    }

    .form-control::placeholder {
      color: #ccc;
    }

    .btn-primary {
      background-color: #FE3E4D;
      border-color: #FE3E4D;
    }

    .btn-link {
      color: #ffffff;
      text-decoration: underline;
    }

    .btn-link:hover {
      color: #ccc;
    }

    h2 {
      font-family: 'Montserrat';
      font-size: 50px;
      font-weight: bold;
    }

    input {
      font-family: 'Quicksand', sans-serif;
    }
  </style>
</head>
<body>

  <!-- Form inside dark overlay (no navbar above) -->
  <div class="container-fluid overlay">
    <div class="container">
      <div class="text-center mb-4">
        <h2>Add New Event</h2>
      </div>

      <form action="/addEvent" method="POST">
        <div class="mb-3">
          <label for="name" class="form-label" style="font-family: 'Quicksand', sans-serif; font-weight: bold;">Event Name:</label>
          <input type="text" class="form-control" id="name" name="name" style="font-family: 'Quicksand', sans-serif;" required />
        </div>

        <div class="mb-3">
          <label for="date" class="form-label" style="font-family: 'Quicksand', sans-serif; font-weight: bold;">Event Date:</label>
          <input type="date" class="form-control" id="date" name="date" style="font-family: 'Quicksand', sans-serif;" required />
        </div>

        <div class="mb-3">
          <label for="location" class="form-label" style="font-family: 'Quicksand', sans-serif; font-weight: bold;">Location:</label>
          <input type="text" class="form-control" id="location" name="location" style="font-family: 'Quicksand', sans-serif;" required />
        </div>

        <div class="mb-3">
          <label for="description" class="form-label" style="font-family: 'Quicksand', sans-serif; font-weight: bold;">Description:</label>
          <textarea id="description" name="description" class="form-control" rows="4" style="font-family: 'Quicksand', sans-serif;" required></textarea>
        </div>

        <button type="submit" class="btn btn-primary" style="font-family: 'Quicksand', sans-serif;">Add Event</button>
      </form>

      <div class="text-center mt-4">
        <a href="/events" class="btn btn-link" style="font-family: 'Quicksand', sans-serif;">Back to Events</a>
      </div>
    </div>
  </div>

</body>
</html>
