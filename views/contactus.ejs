<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Contact Us - Singapore IG Connect</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet" />

  <style>
    /* Background image with fade-in animation */
    body {
      font-family: 'Segoe UI', sans-serif;
      background-image: url('/image/events_background4.png');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      animation: fadeInBg 3s ease-in-out forwards;
      position: relative;
    }

    @keyframes fadeInBg {
      0% {
        opacity: 0;
        transform: scale(1.1);
      }
      100% {
        opacity: 1;
        transform: scale(1);
      }
    }

    /* Dark transparent overlay for whole section */
    .section {
      background-color: rgba(33, 33, 33, 0.8); /* Dark grey transparent */
      color: white;
      padding: 100px 20px;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    /* Header size and style */
    h2 {
      font-weight: bold;
      font-size: 400%; /* ✅ 100% bigger than normal */
    }

    /* Paragraph style */
    p {
      font-weight: bold;
      font-family: 'Cambria Math', 'Arial', sans-serif;
    }

    /* Slide animations */
    .slide-in-left {
      opacity: 0;
      transform: translateX(-100px);
      animation: slideInLeft 1.5s ease forwards;
    }

    .slide-in-right {
      opacity: 0;
      transform: translateX(100px);
      animation: slideInRight 1.5s ease forwards;
    }

    @keyframes slideInLeft {
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes slideInRight {
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    .highlight {
      color: #FE3E4D;
    }

    footer {
      background-color: #212529;
      color: white;
      padding: 20px 0;
    }
  </style>
</head>
<body>

  <!-- Contact Section -->
  <div class="container-fluid section">
    <div class="container">
      <div class="row align-items-center">
        <!-- Paragraph on Left -->
        <div class="col-md-6 slide-in-left">
          <p class="fs-5">
            Have a question, suggestion, or just want to say hello? <br><br>
            We’d love to hear from you! Whether you're a student, IG leader, or curious visitor, don’t hesitate to drop us a message. Let’s connect and build something amazing together.
          </p>
          <p>Contact us at <a href="https://mail.google.com/mail/?view=cm&fs=1&to=youremail@example.com" target="_blank">Singapore IG Connect</a></p>

                  <div class="mt-4">
          <a href="/" class="btn btn-danger btn-lg">back</a>
        </div>


        </div>

        <!-- Header on Right -->
        <div class="col-md-6 text-end slide-in-right">
          <h2>Contact <span class="highlight">Singapore IG Connect</span></h2>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="text-center">
    &copy; 2025 Singapore IG Connect · All rights reserved
  </footer>

  <!-- AOS Animation Scripts -->
  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>
    AOS.init({
      duration: 600,
      once: true
    });
  </script>

</body>
</html>
