<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Quicksand&display=swap" rel="stylesheet">
</head>
<style>
    body {
        background-image: url('/image/coverpage_background.png'); 
        background-size: cover;        
        background-repeat: no-repeat;   
        background-position: center;  
    }

    h1 {
        font-size: 60px;
        font-family: 'Montserrat';
        font-weight: bold;
    }

    h3 {
        font-weight: normal;
        font-size: 20px;
        font-family: 'Quicksand', sans-serif;
    }

    .top-center-text {
            position: absolute;
            top: 140px; 
            left: 50%;
            transform: translateX(-50%);
            text-align: center;
            width: 100%;
    }

    .move-buttons-up {
    margin-top: -50px; 
    }

    body {
        font-family: 'Quicksand', sans-serif;
    }

    .btn {
        font-family: 'Quicksand', sans-serif;
    }
</style>
<body>

    <%- include('partials/navbar') %>
    
    <div class="top-center-text">
        <h1>Welcome to Singapore IG Connect</h1>
        <h3>Students and admins, sign in to manage or explore interest groups.</h3>
    </div>

    <div class="d-flex justify-content-center align-items-center min-vh-100">
        <div class="text-center move-buttons-up">
            <% if (messages && messages.length > 0 && user) { %>
                <h1 class="fw-bold mb-4">
                    <%= messages[0] %>
                </h1>

                <h3>Hello, <%= user.username %></h3>

                <a href="/logout" class="btn btn-primary mt-4">Logout</a>

            <% } else { %>
                <a href="/register" class="btn btn-primary me-2">Register</a>
                <a href="/login" class="btn btn-primary">Login</a>
            <% } %>

        </div>
    </div>
</body>
</html>